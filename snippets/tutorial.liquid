{% comment %}
  it doesn't affect using the - after the %, te only thin that
  changes is that if we didn't use it, it will add an empty line
  before and after we print a variable
{% endcomment %}

{% assign my_variable = "hello World of accel" %}
{{ my_variable }}